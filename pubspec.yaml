name: habit_tracker
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.3
  cloud_firestore: ^5.2.1
  provider: ^6.0.3
  fl_chart: ^0.68.0
  shared_preferences: ^2.1.0
  flutter_local_notifications: ^17.2.1+2
  flutter_localizations:
    sdk: flutter
  table_calendar: ^3.0.7
  easy_localization: ^3.0.0
  intl: ^0.19.0
  timezone: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^4.0.0  # Add this line

flutter:
  uses-material-design: true

